# Skillforge

A content ingestion and curation engine that sends weekly emails with top updates to subscribers.

## Usage

1. Install the project using `poetry install`.
2. Run the project using `poetry run python src/skillforge.py`.
3. Run tests using `poetry run pytest tests`.
